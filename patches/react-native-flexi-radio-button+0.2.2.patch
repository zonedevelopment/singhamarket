diff --git a/node_modules/react-native-flexi-radio-button/lib/context.js b/node_modules/react-native-flexi-radio-button/lib/context.js
new file mode 100644
index 0000000..8d8378a
--- /dev/null
+++ b/node_modules/react-native-flexi-radio-button/lib/context.js
@@ -0,0 +1,11 @@
+import React from 'react'
+
+const RadioGroupContext = React.createContext({
+  onSelect: () => {},
+  size: 20,
+  thickness: 1,
+  color: '#007AFF',
+  highlightColor: null,
+})
+
+export default RadioGroupContext
diff --git a/node_modules/react-native-flexi-radio-button/lib/radioButton.js b/node_modules/react-native-flexi-radio-button/lib/radioButton.js
index 276a288..00badb2 100644
--- a/node_modules/react-native-flexi-radio-button/lib/radioButton.js
+++ b/node_modules/react-native-flexi-radio-button/lib/radioButton.js
@@ -1,11 +1,11 @@
 import React, { Component } from 'react'
-import PropTypes from 'prop-types'
 import {
   StyleSheet,
   View,
   Text,
   TouchableWithoutFeedback
 } from 'react-native';
+import RadioGroupContext from './context'
 
 export default class RadioButton extends Component{
     constructor(props, context){
@@ -19,21 +19,30 @@ export default class RadioButton extends Component{
 	}
 
     getRadioStyle(){
+        const size = Number.isFinite(this.context.size) ? this.context.size : 20
+        const thickness = Number.isFinite(this.context.thickness) ? this.context.thickness : 1
+        const color = this.props.isSelected && this.props.activeColor
+            ? this.props.activeColor
+            : (this.context.color || '#007AFF')
+
         return {
-            height: this.context.size,
-	        width: this.context.size,
-	        borderRadius: this.context.size / 2,
-	        borderWidth: this.context.thickness,
-	        borderColor: this.props.isSelected && this.props.activeColor?this.props.activeColor:this.context.color,
+            height: size,
+	        width: size,
+	        borderRadius: size / 2,
+	        borderWidth: thickness,
+	        borderColor: color,
         }
     }
 
     getRadioDotStyle(){
+        const size = Number.isFinite(this.context.size) ? this.context.size : 20
+        const color = this.props.color || this.props.activeColor || this.context.color || '#007AFF'
+
         return {
-            height: this.context.size / 2,
-            width: this.context.size / 2,
-            borderRadius: this.context.size / 4,
-            backgroundColor: this.props.color || this.props.activeColor,
+            height: size / 2,
+            width: size / 2,
+            borderRadius: size / 4,
+            backgroundColor: color,
         }
     }
 
@@ -62,14 +71,7 @@ export default class RadioButton extends Component{
         )
     }
 }
-
-RadioButton.contextTypes = {
-        onSelect: PropTypes.func.isRequired,
-        size: PropTypes.number.isRequired,
-        thickness: PropTypes.number.isRequired,
-        color: PropTypes.string.isRequired,
-        highlightColor: PropTypes.string
-    }
+RadioButton.contextType = RadioGroupContext
 
 let styles = StyleSheet.create({
   container:{
diff --git a/node_modules/react-native-flexi-radio-button/lib/radioGroup.js b/node_modules/react-native-flexi-radio-button/lib/radioGroup.js
index 371d7cd..2623b65 100644
--- a/node_modules/react-native-flexi-radio-button/lib/radioGroup.js
+++ b/node_modules/react-native-flexi-radio-button/lib/radioGroup.js
@@ -1,5 +1,4 @@
 import React, { Component } from 'react'
-import PropTypes from 'prop-types'
 import {
   StyleSheet,
   View,
@@ -7,6 +6,7 @@ import {
 } from 'react-native';
 
 import RadioButton from './radioButton'
+import RadioGroupContext from './context'
 
 const defaultSize = 20
 const defaultThickness = 1
@@ -32,16 +32,6 @@ export default  class RadioGroup extends Component{
         }
 	}
 
-    getChildContext() {
-        return {
-            onSelect: this.onSelect ,
-            size: this.props.size,
-            thickness: this.props.thickness,
-            color: this.props.color,
-            highlightColor: this.props.highlightColor
-        };
-    }
-
     onSelect(index, value){
         this.setState({
             selectedIndex: index
@@ -51,6 +41,17 @@ export default  class RadioGroup extends Component{
     }
 
     render(){
+        const size = Number.isFinite(this.props.size) ? this.props.size : defaultSize
+        const thickness = Number.isFinite(this.props.thickness) ? this.props.thickness : defaultThickness
+        const color = this.props.color || defaultColor
+        const contextValue = {
+            onSelect: this.onSelect,
+            size,
+            thickness,
+            color,
+            highlightColor: this.props.highlightColor,
+        }
+
         var radioButtons = React.Children.map(this.props.children, (radioButton, index) => {
             let isSelected = this.state.selectedIndex == index
             let color = isSelected && this.props.activeColor?this.props.activeColor:this.props.color
@@ -68,22 +69,15 @@ export default  class RadioGroup extends Component{
         })
 
         return(
-            <View style={this.props.style}>
-                {radioButtons}
-            </View>
+            <RadioGroupContext.Provider value={contextValue}>
+                <View style={this.props.style}>
+                    {radioButtons}
+                </View>
+            </RadioGroupContext.Provider>
         )
     }
 }
 
-RadioGroup.childContextTypes = {
-    onSelect: PropTypes.func.isRequired,
-    size: PropTypes.number.isRequired,
-    thickness: PropTypes.number.isRequired,
-    color: PropTypes.string.isRequired,
-    activeColor: PropTypes.string,
-    highlightColor: PropTypes.string,
-}
-
 RadioGroup.defaultProps = {
     size: defaultSize,
     thickness: defaultThickness,
